
Lucid .Net
==========

Build commands:

b                   : build
b /t:clean          : clean
b /t:push           : Push Lucid packages to NuGet and publish them (setApiKey before running this)

AppHarbor\Push.bat  : Push to appharbor remote
AppHarbor\Build.bat : Run the build using the same parameters as AppHarbor
