
Lucid .Net
==========

Build commands:

b                   : build
b /t:clean          : clean
b /t:push           : Push Lucid packages to NuGet and publish them (setApiKey before running this)
