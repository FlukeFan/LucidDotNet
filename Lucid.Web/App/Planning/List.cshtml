@inherits LucidPage<IList<DesignDto>>
@using Lucid.Web.App.Planning
@{
    SetLayout(title: "Designs");
}
<div>Design</div>

<ul id="designList">
    @foreach (var design in Model)
    {
        <li>Design: @design.Name</li>
    }
</ul>

<div>
    <a href="@Url.Content(Actions.StartDesign())">Start Design</a>
</div>
